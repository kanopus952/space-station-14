<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      SetSize="320 180"
                      MinSize="320 180"
                      Title="{Loc 'pirate-pallet-console-menu-title'}">
    <BoxContainer Orientation="Vertical" Margin="5">
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'pirate-pallet-menu-appraisal-label'}"
                   StyleClasses="LabelKeyText" />
            <Label Name="AppraisalLabel"
                   Text="{Loc 'pirate-pallet-menu-no-goods-text'}" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'pirate-pallet-menu-doubloons-label'}"
                   StyleClasses="LabelKeyText" />
            <Label Name="DoubloonsLabel"
                   Text="0" />
        </BoxContainer>
        <BoxContainer Orientation="Horizontal">
            <Label Text="{Loc 'pirate-pallet-menu-count-label'}"
                   StyleClasses="LabelKeyText" />
            <Label Name="CountLabel"
                   Text="{Loc 'pirate-pallet-menu-no-goods-text'}" />
        </BoxContainer>
        <Button Name="AppraiseButton"
                Text="{Loc 'pirate-pallet-appraise-button'}"/>
        <Button Name="SellButton"
                Text="{Loc 'pirate-pallet-sell-button'}"/>
        <TextureButton VerticalExpand="True" />
    </BoxContainer>
</controls:FancyWindow>
